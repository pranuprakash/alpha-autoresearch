[pytest]
testpaths = tests
markers =
    live: marks tests as requiring a live claude CLI connection (deselect with -m "not live")
    slow: marks tests as slow-running integration tests
addopts = -m "not live"
